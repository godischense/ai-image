不要输出思考过程，不要说你好，谢谢，抱歉等客套话，直接给结论。
components文件夹下的组件，不要将不是组件里的功能和逻辑写在组件中，而是在组件的父组件中实现，组件之间通过props和emits进行通信。
只要使使用py文件，都是用本地嵌入式的环境运行，路径为./evns/
但后端不要直接重启，而是提醒我，让我手动重启后端服务；
所有需要提示性弹窗的地方，都必须使用confirmDialog组件，不能直接使用alert等弹窗。
所有新的功能，必须使用单独的py文件，不要将其写在原本的文件中，除非功能与文件中的功能相关，否则必须使用单独的py文件。
在编写代码或者修改代码时，必须在py文件中添加注释，注释中必须包含功能的描述，以及功能的实现逻辑。
在实现一个功能时，要优先考虑失败的情况，而不是成功的情况，例如一个功能在执行过程中，可能会因为某种原因失败，需要在失败时进行处理，而不是在成功时进行处理。
在实现一个功能时，要先问问自己，用户可能会出现哪些操作，以及这些操作的后果，例如一个功能在执行过程中，可能会因为某种原因失败，需要在失败时进行处理，而不是在成功时进行处理。
在实现一个功能时，要先问问自己，这个功能在执行的过程中会出现哪些异常情况，以及这些异常情况的后果，每一种情况都需要处理，且必须有兜底方案。
如果是一个永久性的功能，那么在实现时，需要考虑，用户可能会在执行的过程中，中断这个功能，以及中断后，是否需要继续执行，用户是否需要手动重启这个功能，重启后如何继续执行，是接着中断处继续执行，还是从头开始执行，离开执行功能的页面，该功能是否可以在后台继续执行，并且可以将执行的过程实时展示给用户。
如果是一个临时性的功能，那么在实现时，需要考虑，用户可能会在执行的过程中，中断这个功能，以及中断后，是否需要继续执行。
本项目中的生图功能，异步生图，异步查询，异步编辑，对话生图，对话编辑图片，都需要参考新建文件夹下的几个md文档。
在写代码功能介绍时，不要将功能文字说明写在代码中间，例如：
```python
def add_task(task_id: str, data_json: str):
    """
    添加一个任务到数据库
    """
```
而是将功能文字说明使用#写在代码的上面，例如：
```python
# 添加一个任务到数据库
def add_task(task_id: str, data_json: str):
```
